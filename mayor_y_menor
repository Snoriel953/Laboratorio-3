numeros = []

for i in range(5):
    num = float(input(f"Número {i+1}: "))
    numeros.append(num)          # Agregamos cada número a la lista

print(f"Números: {numeros}")
print(f"El mayor es: {max(numeros)}")
print(f"El menor es: {min(numeros)}")
